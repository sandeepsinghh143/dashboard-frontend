"use client";

import { setLineChartData } from "@/redux/chartSlice";

const { useDispatch } = require("react-redux");

export const chartDispatchHandler = (type, data) => {
  //   const dispatch = useDispatch();
  //   dispatch(setLineChartData(data));
};
